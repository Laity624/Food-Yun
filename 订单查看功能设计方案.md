# 订单查看功能设计方案

## 功能概述

订单查看功能是点餐系统的重要组成部分，用户可以通过多个入口查看和管理自己的订单。基于项目设计，订单查看主要通过**订单列表页面**实现，提供完整的订单管理体验。

## 订单查看入口

### 1. 主要入口 - 底部导航栏
**位置**：底部导航栏的"订单"标签页
**路径**：`pages/order-list/order-list`
**特点**：最直接的订单查看入口，用户习惯性操作

### 2. 次要入口 - 点餐页面购物车
**位置**：点餐页面右上角购物车图标
**功能**：查看当前购物车状态和最近订单
**特点**：便捷的当前订单状态查看

### 3. 备选入口 - 个人中心
**位置**：个人中心页面的"我的订单"入口
**功能**：作为订单管理的备选入口
**特点**：符合用户对个人中心功能的预期

## 订单列表页面设计

### 页面布局结构
```
┌─────────────────────────┐
│ ← 我的订单        🔍搜索 │ ← 头部导航
├─────────────────────────┤
│ 全部 待制作 制作中 已完成 │ ← 状态筛选栏
├─────────────────────────┤
│ 📱 订单列表 (可滚动)    │ ← 主要内容区
│ ┌─────────────────────┐ │
│ │ 🌅 早餐订单 [制作中] │ │ ← 订单卡片
│ │ 🍖 红烧肉 (小王)    │ │
│ │   45分钟 ⭐4.8      │ │
│ │ 🥚 蒸蛋羹 (小王)    │ │
│ │   20分钟 ⭐4.7      │ │
│ │ ─────────────────── │ │
│ │ 2024-09-17 08:30   │ │
│ │ [查看详情] [再次点餐]│ │
│ └─────────────────────┘ │
│ ┌─────────────────────┐ │
│ │ 🌞 午餐订单 [已完成] │ │
│ │ 🍅 番茄鸡蛋 (小李)  │ │
│ │   15分钟 ⭐4.6      │ │
│ │ 🍗 宫保鸡丁 (小张)  │ │
│ │   30分钟 ⭐4.9      │ │
│ │ ─────────────────── │ │
│ │ 2024-09-16 12:00   │ │
│ │ [查看详情] [再次点餐]│ │
│ └─────────────────────┘ │
│ ...                     │
├─────────────────────────┤
│ 📄 加载更多...          │ ← 分页加载
└─────────────────────────┘
```

### 订单卡片设计

**卡片信息结构：**
```
┌─────────────────────────────────┐
│ 🌅 早餐订单              [制作中] │ ← 餐次 + 状态
├─────────────────────────────────┤
│ 🍖 红烧肉 (小王)         45分钟  │ ← 菜品信息
│ 🥚 蒸蛋羹 (小王)         20分钟  │
├─────────────────────────────────┤
│ 2024-09-17 08:30               │ ← 订单时间
│ [查看详情] [再次点餐]           │ ← 操作按钮
└─────────────────────────────────┘
```

**信息元素：**
- **餐次图标**：🌅早餐、🌞午餐、🌙晚餐
- **订单状态**：待制作、制作中、已完成、已取消
- **菜品信息**：图片、名称、作者、制作时间、评分
- **订单时间**：创建时间或完成时间
- **操作按钮**：查看详情、再次点餐、开始制作

## 订单状态管理

### 状态定义
```javascript
const ORDER_STATUS = {
  PENDING: 'pending',      // 待制作
  COOKING: 'cooking',      // 制作中
  COMPLETED: 'completed',  // 已完成
  CANCELLED: 'cancelled'   // 已取消
}
```

### 状态流转
```
待制作 → 制作中 → 已完成
   ↓       ↓       ↓
 取消    完成    评价
```

### 状态筛选
- **全部**：显示所有状态的订单
- **待制作**：显示待制作的订单
- **制作中**：显示正在制作的订单
- **已完成**：显示已完成的订单

## 订单详情页面设计

### 详情页面信息
```
┌─────────────────────────┐
│ ← 订单详情              │ ← 头部导航
├─────────────────────────┤
│ 🌅 早餐订单 - 制作中     │ ← 订单标题
│ 订单号：202409170830001  │
├─────────────────────────┤
│ 菜品详情：              │
│ ┌─────────────────────┐ │
│ │ 🍖 红烧肉          │ │ ← 菜品详情
│ │ 作者：小王 ⭐4.8    │ │
│ │ 制作时间：45分钟    │ │
│ │ 难度：中等          │ │
│ └─────────────────────┘ │
│ ┌─────────────────────┐ │
│ │ 🥚 蒸蛋羹          │ │
│ │ 作者：小王 ⭐4.7    │ │
│ │ 制作时间：20分钟    │ │
│ │ 难度：简单          │ │
│ └─────────────────────┘ │
├─────────────────────────┤
│ 订单信息：              │
│ 创建时间：2024-09-17 08:30│
│ 预计完成：2024-09-17 09:35│
│ 备注：少油少盐          │
├─────────────────────────┤
│ [开始制作] [取消订单]    │ ← 操作按钮
└─────────────────────────┘
```

## 技术实现方案

### 1. 数据结构设计

**订单数据结构：**
```javascript
// 订单主表
{
  _id: ObjectId,
  userId: ObjectId,
  orderNo: String,           // 订单号
  mealType: String,          // 餐次类型
  orderDate: Date,           // 点餐日期
  recipes: [{
    recipeId: ObjectId,
    recipeName: String,
    authorId: ObjectId,
    authorName: String,
    authorAvatar: String,
    image: String,
    preparationTime: String,
    difficulty: String,
    rating: Number
  }],
  status: String,            // 订单状态
  notes: String,             // 备注
  totalRecipes: Number,      // 总菜谱数量
  estimatedTime: Number,     // 预计制作时间
  createdAt: Date,
  updatedAt: Date
}
```

### 2. 云函数设计

**获取订单列表：**
```javascript
// 云函数: getOrderList
{
  action: 'getOrderList',
  status: String,    // 可选筛选状态
  page: Number,      // 页码
  limit: Number      // 每页数量
}
```

**获取订单详情：**
```javascript
// 云函数: getOrderDetail
{
  action: 'getOrderDetail',
  orderId: String    // 订单ID
}
```

**更新订单状态：**
```javascript
// 云函数: updateOrderStatus
{
  action: 'updateOrderStatus',
  orderId: String,   // 订单ID
  status: String     // 新状态
}
```

### 3. 前端页面实现

**订单列表页面 (order-list.wxml)：**
```xml
<view class="order-list-container">
  <!-- 头部导航 -->
  <view class="header">
    <text class="title">我的订单</text>
    <van-icon name="search" bind:click="onSearch" />
  </view>
  
  <!-- 状态筛选 -->
  <view class="filter-tabs">
    <view 
      wx:for="{{statusTabs}}" 
      wx:key="value"
      class="tab {{currentStatus === item.value ? 'active' : ''}}"
      bind:click="onStatusChange"
      data-status="{{item.value}}"
    >
      {{item.label}}
    </view>
  </view>
  
  <!-- 订单列表 -->
  <scroll-view class="order-list" scroll-y="true">
    <view 
      wx:for="{{orders}}" 
      wx:key="_id"
      class="order-card"
      bind:click="onOrderClick"
      data-order="{{item}}"
    >
      <!-- 订单卡片内容 -->
    </view>
  </scroll-view>
</view>
```

**订单详情页面 (order-detail.wxml)：**
```xml
<view class="order-detail-container">
  <!-- 订单基本信息 -->
  <view class="order-header">
    <text class="meal-type">{{order.mealType}}</text>
    <text class="status">{{order.status}}</text>
  </view>
  
  <!-- 菜品详情 -->
  <view class="recipes-section">
    <view 
      wx:for="{{order.recipes}}" 
      wx:key="recipeId"
      class="recipe-item"
    >
      <!-- 菜品信息 -->
    </view>
  </view>
  
  <!-- 操作按钮 -->
  <view class="action-buttons">
    <van-button 
      wx:if="{{order.status === 'pending'}}"
      type="primary"
      bind:click="onStartCooking"
    >
      开始制作
    </van-button>
  </view>
</view>
```

### 4. 交互功能实现

**状态筛选：**
```javascript
onStatusChange(e) {
  const status = e.currentTarget.dataset.status
  this.setData({
    currentStatus: status,
    orders: [],
    page: 1
  })
  this.loadOrders()
}
```

**订单加载：**
```javascript
loadOrders() {
  wx.cloud.callFunction({
    name: 'order',
    data: {
      action: 'getOrderList',
      status: this.data.currentStatus,
      page: this.data.page,
      limit: 10
    },
    success: (res) => {
      if (res.result.success) {
        this.setData({
          orders: [...this.data.orders, ...res.result.data.orders],
          hasMore: res.result.data.hasMore
        })
      }
    }
  })
}
```

## 用户体验设计

### 1. 加载状态
- **初始加载**：显示骨架屏
- **分页加载**：底部显示"加载更多"
- **刷新加载**：下拉刷新

### 2. 空状态
- **无订单**：显示引导文案和点餐按钮
- **筛选无结果**：显示"暂无该状态订单"

### 3. 错误处理
- **网络错误**：显示重试按钮
- **数据错误**：显示友好错误信息

### 4. 操作反馈
- **状态更新**：显示成功提示
- **操作确认**：重要操作需要确认

## 开发计划

### 第一阶段：基础页面开发 (1天)
- [ ] 订单列表页面布局
- [ ] 订单卡片组件
- [ ] 状态筛选功能
- [ ] 基础交互逻辑

### 第二阶段：详情页面开发 (1天)
- [ ] 订单详情页面
- [ ] 菜品详情展示
- [ ] 操作按钮功能
- [ ] 状态更新逻辑

### 第三阶段：功能完善 (1天)
- [ ] 搜索功能
- [ ] 分页加载
- [ ] 错误处理
- [ ] 用户体验优化

## 总结

订单查看功能通过**订单列表页面**提供完整的订单管理体验：

1. **多入口设计**：底部导航、购物车、个人中心
2. **状态管理**：待制作、制作中、已完成、已取消
3. **筛选功能**：按状态筛选订单
4. **详情查看**：完整的订单和菜品信息
5. **操作功能**：开始制作、取消订单、再次点餐

用户下单后，可以通过底部导航栏的"订单"标签页查看所有订单，支持按状态筛选和搜索，提供完整的订单管理体验。

